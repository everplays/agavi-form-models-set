{{#t}}
<script language="javascript">
    jQuery.validator.addClassRules("rules-{{idPrefix}}{{id}}", {
    {{#required}}
        required: true,
    {{/required}}
    });
</script>
<div class="{{elementClass}}" id="{{idPrefix}}{{id}}_container">
    {{#title}}<label for="{{idPrefix}}{{id}}">{{.}}:</label>{{/title}}
    <div class="input">
        <div class="input-append">
            <input type="text" name="{{name}}_view" id="{{idPrefix}}{{id}}_view" {{#view}}value="{{.}}"{{/view}} {{#readonly}}readonly="readonly"{{/readonly}} {{#disabled}}disabled="disabled"{{/disabled}} class="rules-{{idPrefix}}{{id}}" />
            <input type="hidden" name="{{name}}" id="{{idPrefix}}{{id}}" value="{{value}}"/>
            <label class="add-on">↓</label>
        </div>
    </div>
</div>
{{/t}}
