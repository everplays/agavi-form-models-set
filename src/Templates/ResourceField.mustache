{{#t}}
<div class="{{elementClass}}" id="{{idPrefix}}{{id}}_container\">";
    <label for="{{idPrefix}}{{id}}">{{title}}:</label>
    <div class="input">
        <div class="input-append">
            <input type="text" name="{{name}}_view" id="{{idPrefix}}{{id}}_view" {{#view}}value="{{.}}"{{/view}} {{#readonly}}readonly="readonly"{{/readonly}} {{#disabled}}disabled="disabled"{{/disabled}} />
            <input type="hidden" name="{{name}}" id="{{idPrefix}}{{id}}" value="{{value}}" />
            <label class="add-on">↓</label>
        </div>
    </div>
</div>
{{/t}}
