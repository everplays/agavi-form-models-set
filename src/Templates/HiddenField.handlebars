{{#with t}}
<input type="hidden" name="{{name}}" id="{{idPrefix}}{{id}}" value="{{value}}" {{#if max}}maxlength="{{max}}"{{/if}}{{#if readonly}} readonly="readonly"{{/if}}{{#if disabled}} disabled="disabled"{{/if}} />
{{/with}}
