{{#t}}
<div class="{{elementClass}}" id="{{idPrefix}}{{id}}_container">
    <label for="{{idPrefix}}{{id}}">{{title}}:</label>
    <div class="input">
        <input type="password" name="{{name}}" id="{{idPrefix}}{{id}}" value="{{value}}" {{#max}}maxlength="{{.}}"{{/max}} {{#readonly}}readonly="readonly"{{/readonly}} {{#disabled}}disabled="disabled"{{/disabled}} />
    </div>
</div>
{{/t}}
